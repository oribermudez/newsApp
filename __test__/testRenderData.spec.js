import { renderData } from "../src/client/js/renderData"

  describe("Testing the renderData functionality", () => {
    test("Testing the renderData() function", () => {
        expect(renderData).toBeDefined();
    });
});